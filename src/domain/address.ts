/**
 * 배송지 규칙 — 서버(customer.service)와 화면(마이페이지) 양쪽이 같은 값을 봐야 하는 것만 둔다.
 *
 * 서비스에 두면 화면이 못 읽는다(server-only). 그렇다고 화면에 5를 적어 두면
 * 서버 상한을 바꿀 때 화면만 옛 숫자를 안내하게 된다 — 규칙이 갈라지는 대신 여기 한 벌만 둔다.
 */

/**
 * 배송지 보관 개수 상한.
 *
 * 무제한이면 주문할 때마다 쌓여(특히 선물 배송) 체크아웃 선택 목록이 오히려 불편해진다.
 * 화면에서만 막으면 우회되므로 판정은 서버가 한다 — 화면은 미리 알려줄 뿐이다.
 */
export const MAX_SAVED_ADDRESSES = 5;
