/**
 * `server-only` 대체 스텁 — 배치 번들 전용.
 *
 * 원본 패키지는 "클라이언트 번들에 섞이면 빌드를 깨뜨린다"는 Next 전용 표식이고,
 * 그 목적은 esbuild로 만드는 배치(Node에서 단독 실행)에는 해당하지 않는다.
 * 비워 두면 그만이지만, 원본을 그대로 두면 react-server 조건이 없다며 실행 시 죽는다.
 */
module.exports = {};
