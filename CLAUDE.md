# CLAUDE.md — PaRaSOL(파라솔) 프로젝트 규칙

장애인 생산품 B2C 쇼핑몰 커스텀 구축 (재판매 코어 제품). 단일 상점 몰이며, 완성 후 테마 토큰·site_setting 교체로 타 업체에 리스킨 판매한다.

---

## [참조 문서] — 스펙·디자인 원본 (프로젝트 폴더 외부에 있음)

| 문서 | 경로 |
|---|---|
| 구축 스펙서 (기능·일정·정책) | `C:\_Hope\PaRaSOL\PaRaSOL 브랜드 메인 화면_핸드오프\mall-spec.md` |
| DB 스키마 (Drizzle 53테이블 v0.3) | `C:\_Hope\PaRaSOL\PaRaSOL 브랜드 메인 화면_핸드오프\schema.ts` |
| ERD | `C:\_Hope\PaRaSOL\PaRaSOL 브랜드 메인 화면_핸드오프\erd.mermaid` |
| 디자인 핸드오프 가이드 (토큰·컴포넌트·UX 규칙) | `C:\_Hope\PaRaSOL\PaRaSOL 브랜드 메인 화면_핸드오프\design_handoff_parasol\README.md` |
| 디자인 토큰 정리본 | `C:\_Hope\PaRaSOL\PaRaSOL 브랜드 메인 화면_핸드오프\design_handoff_parasol\tokens-reference.css` |
| 화면 목업 44종 | `C:\_Hope\PaRaSOL\PaRaSOL 브랜드 메인 화면_핸드오프\design_handoff_parasol\screens\*.dc.html` |

- `.dc.html`은 **디자인 레퍼런스(고충실도 프로토타입)이며 프로덕션 코드가 아니다** — 복사 대상이 아니라 재구현 대상.
- 화면 구현 착수 시: 해당 `.dc.html` + 핸드오프 README의 UX 규칙·탭 순서 주석을 먼저 확인한다.
- 오버레이(토스트·모달·바텀시트 등)는 `PaRaSOL 오버레이모음.dc.html`이 마스터 레퍼런스.

## [스택]

Next.js(App Router) **단일 앱** — 라우트 그룹으로 스토어프론트/관리자 분리 · tRPC · Drizzle ORM + PostgreSQL · shadcn/ui(Radix) + Tailwind · 토스페이먼츠 결제위젯 · 소셜 로그인(카카오·네이버·구글) · 솔라피 알림톡

**확정 버전 (2026-07-23)**: Next.js 16.2.11 (Turbopack) · React 19.2 · Tailwind 4 · TypeScript 5 · drizzle-orm 0.45 + drizzle-kit 0.31 (드라이버 pg/node-postgres) · Node 22 · PostgreSQL 17. ⚠️ Next.js 16은 학습 데이터와 다를 수 있음 — 구현 전 `node_modules/next/dist/docs/` 및 [AGENTS.md](AGENTS.md) 참조.

**환경 구성**

- **개발**: 앱은 로컬(Windows/IntelliJ)에서 실행 · DB는 명율 서버 PostgreSQL에 **SSH 터널**로 접속(`127.0.0.1:15432` → 서버 5432). 5432 포트 외부 개방 금지.
- **명율 서버** (카페24 가상서버, 공인 IP): **기존 웹서비스가 운영 중 — 건드리지 않는다.** PaRaSOL은 전용 리눅스 계정 `parasol`(sudo 미부여) 아래에 격리. 개발 DB(`parasol` DB / `parasol_app` 롤) + 필요 시 스테이징 웹(별도 포트·별도 PM2 프로세스).
- **운영**: 완성 후 운영서버 별도 구매 → DB 마이그레이션 + 배포 (그 시점에 스펙서 §2의 Nginx·PM2·SSL 구성 적용).
- 서버 셋업 절차서: `C:\_Dev\_md_doc\conlab\projects\PaRaSOL\plan\계획_명율서버개발DB셋업(myeongyul-devdb-setup)_20260723.md`

---

## [RULE-1] 환경설정 파일 접근 금지

**DB 접속 정보·PG 시크릿·API 키가 포함된 환경설정 파일은 절대 읽거나 열지 않는다.**

대상 파일 패턴 (읽기·수정·참조 모두 금지):
- `.env` / `.env.*` (`.env.local`, `.env.production` 등)

위 파일이 필요한 상황이 생기면, 필요한 설정 **키 이름만** 사용자에게 요청한다. 값이 비어 있는 템플릿(`.env.example`)의 작성·수정은 허용.

**실수로 읽었을 경우 처리 절차:**
1. 즉시 사용자에게 해당 파일을 읽었음을 알린다.
2. 읽은 내용(DB URL, 계정, 비밀번호, API 키 등 민감 정보)을 응답에 노출하지 않는다.
3. 이후 해당 파일에 대한 모든 접근을 거부(deny)한다.

---

## [RULE-2] DB 직접 접근 및 SQL 명령 실행 금지

**메인 에이전트·서브에이전트 모두, 어떤 상황에서도 데이터베이스에 직접 연결하거나 DDL/DML/DCL 등 모든 SQL 명령을 실행하지 않는다.**

- 스키마 변경, 데이터 조회·삽입·수정·삭제가 필요한 경우: SQL 스크립트를 **텍스트로 작성하여 사용자에게 전달**한다.
- 사용자가 직접 DB 툴을 통해 실행하고, Claude는 실행 결과를 전달받아 다음 단계를 진행한다.
- Drizzle 관련: DB 연결 없이 파일만 생성하는 `drizzle-kit generate`는 허용. **`drizzle-kit push` / `drizzle-kit migrate` 등 DB에 접속하는 명령은 금지** — 생성된 마이그레이션 SQL을 사용자가 직접 실행한다.
- **스킬이 서브에이전트를 통해 "꼭 필요한 작업"이라고 판단하더라도 DB 직접 작업은 절대 실행하지 않는다.**

---

## [RULE-3] 허용 언어 제한

**사용자가 명시적으로 요청하지 않은 언어로 코드를 생성하거나 추천하지 않는다.**

- 이 프로젝트의 허용 언어: **TypeScript(Next.js/Node), JavaScript, SQL(PostgreSQL), HTML/CSS**
- Python 등 위 언어 외의 코드 생성이 유리하다고 판단되더라도, 먼저 사용자에게 언어 선택을 질문한다.

---

## [RULE-4] 작업 전 계획 수립 및 완료 후 리뷰 의무

**모든 작업은 아래 순서를 반드시 따른다.**

```
1. 계획(Plan) 수립   → 사용자에게 제시
2. 사용자 검토·승인  → 승인 확인 후에만 진행
3. 작업 실행
4. 완료 후 리뷰     → 변경 내용 요약 및 이슈 보고
5. 사용자 최종 확인 → 완료 처리
```

계획 없이 바로 코드를 작성하거나 파일을 수정하지 않는다.

---

## [RULE-5] 파일 삭제·수정 시 명시적 승인 필수

**파일의 삭제, 이름 변경, 내용 수정은 어떤 모드(자동·수동)에서도 반드시 사용자에게 명확히 질문하고 승인을 받은 후 처리한다.**

승인 없이 처리할 수 없는 작업:
- 파일 삭제 (`rm`, `Delete`, `Remove-Item` 등)
- 파일 이름 변경 또는 이동
- 기존 파일 내용의 덮어쓰기(Write)
- 설정 파일 수정

승인 요청 형식 예시:
> "다음 파일을 수정하려고 합니다: `apps/web/src/.../ProductDetail.tsx`
> 변경 내용: [구체적 설명]
> 진행해도 될까요?"

---

## [RULE-6] 모든 질문은 한글로

**사용자에게 하는 모든 질문, 확인 요청, 설명은 명확한 한글로 작성한다.**

- 기술 용어(컴포넌트명, 함수명, 파일 경로 등)는 원문 그대로 사용하되, 설명은 한글로 작성한다.
- 애매한 영어 표현 대신 명확한 한국어 문장을 사용한다.

---

## [RULE-7] 형상관리(Git) 명령 실행 및 권유 금지

**메인 에이전트·서브에이전트 모두, Git 등 형상관리 작업은 사용자가 직접 관리한다. Claude는 이를 권유하거나 대신 실행하지 않는다.**

금지 행위:
- `git commit`, `git push`, `git add` 등 변경사항 반영 명령 실행
- "커밋하시겠습니까?" 등의 형상관리 권유
- 브랜치 생성·삭제·병합 등 저장소 구조 변경
- **스킬이 서브에이전트를 통해 "꼭 필요한 작업"이라고 판단하더라도 형상관리 명령은 절대 실행하지 않는다.**

허용 행위:
- 사용자가 명시적으로 요청할 경우 `git log` 등 **히스토리 조회**는 가능하다.
- 사용자가 명시적으로 요청할 경우 마지막 버전과 diff 가능하다.

**Git 상태·히스토리 탐색 제한:**
- 변경 파일 탐색, 커밋 내용 분석 등은 **사용자가 명시적으로 요청한 경우에만** 수행한다.
- 파일 수정일자, `git status` 등을 통한 간접 탐색도 요청 없이는 하지 않는다.
- (참고) CI/CD는 GitHub Actions로 구성 예정 — 워크플로 파일 작성은 가능하나, push·머지는 사용자가 한다.

---

## [RULE-8] 작업 로그 관리 ("작업내용 작성" 명령)

**사용자가 "작업내용 작성"을 요청하면 아래 규칙에 따라 작업 로그 파일을 생성한다.**

### 파일 경로 규칙
```
C:\_Dev\_md_doc\conlab\projects\PaRaSOL\worklog\작업로그_parasolv1_YYYYMMDD.md
```
- `YYYYMMDD`는 요청 당일 날짜 (예: 20260723)
- 같은 날 이미 파일이 존재하면 내용을 **이어서 추가(append)** 한다.
- 형식: **Markdown** (Obsidian 연동 사용)

### 파일 내용 형식
```markdown
---
date: YYYY-MM-DD
project: parasolv1
---

# 작업 로그 — YYYY-MM-DD

## 작업 #N — HH:MM

| 항목 | 내용 |
|------|------|
| 작업 유형 | 기능추가 / 버그수정 / 리팩토링 / 분석 / 기타 |
| 대상 파일 | 수정된 파일 경로 |

### 작업 내용
무엇을 왜 했는지 서술

### 변경 요약
- 변경 사항 1
- 변경 사항 2

### 이슈 / 특이사항
없음 (있을 경우 기록)

---
```

### 참고 디렉토리
- **프로젝트 문서 루트: `C:\_Dev\_md_doc\conlab\projects\PaRaSOL\`** — 위키·로그·계획·분석 등 모든 프로젝트 문서를 이 폴더 하위에 .md로 작성한다 (Obsidian Vault 연동)
- 작업 로그: `C:\_Dev\_md_doc\conlab\projects\PaRaSOL\worklog\`

---

## [RULE-9] 문서 작성 시 인덱스 등록 의무

**작업 문서 또는 분석 문서를 새로 생성할 때, MEMORY.md 문서 인덱스에 키워드 항목을 추가한다.**

인덱스 파일 경로:
```
C:\Users\TSline05\.claude\projects\C---Hope-PaRaSOL-parasolv1\memory\MEMORY.md
```

등록 형식:
```
- `키워드1,키워드2,키워드3` → 문서경로 — 한줄 설명
```

- 키워드는 사용자가 대화에서 쓸 법한 단어로 선정한다 (기능명, 도메인명, 화면명 등)
- MEMORY.md 인덱스 항목 추가는 RULE-5(승인 필수) 예외로, 승인 없이 즉시 추가한다.

---

## [RULE-10] 변수·필드 네이밍 — 대분류격(범용) 이름 자제

**`type`, `data`, `info`, `code`, `status`처럼 대분류격(상위 범주) 단독 이름은 혼란과 충돌을 유발하므로 피한다. 도메인을 한정한 구체적 이름을 쓴다.**

- 예: `code` → `carrierCode`, `type` → `claimType`, `data` → `orderSummary`, `status` → `paymentStatus`.
- 특히 **DTO/API 응답 필드명은 직렬화 JSON 키**가 되어 외부 규격과 충돌할 수 있으므로 더욱 구체적으로 짓는다.
- 스토어 상태 키, 전역 변수, 공통 함수 파라미터처럼 **범위(scope)가 넓을수록** 구체적 접두사를 붙인다.
- **꼭 필요할 때만** 단독 범용 이름을 허용한다 (schema.ts 기존 컬럼명을 따라야 하거나, 범위가 명확히 좁은 지역 변수 등).
- 새 변수/필드/속성 명명 시, 동일·유사 이름이 이미 있는지(충돌 가능성) 먼저 확인한다.

---

## [RULE-11] PaRaSOL 도메인 핵심 규약 (스펙서 §6~7 요약 — 위반 금지)

**아래는 스키마·스펙에 확정된 설계 원칙이다. 구현 코드가 이를 우회하면 안 된다.**

- **금액은 전부 원 단위 integer.** 부동소수점·decimal 문자열 금지.
- **주문은 스냅샷.** order_item에 상품명·가격·옵션을 복사 저장 — 상품이 바뀌어도 주문은 불변. variant 참조는 통계용(nullable).
- **variant가 판매 단위.** 옵션 없는 상품도 variant 1개를 강제 — 카트·주문 로직은 variant만 다룬다.
- **재고 차감은 조건부 UPDATE.** `SET stock = stock - n WHERE stock >= n` 패턴 + inventory_log 기록. 조회 후 차감(read-modify-write) 금지.
- **코드값·slug는 영문 소문자 snake_case.** 한글은 화면 표시용 `name`·label에만 (DB CHECK 제약으로 강제됨).
- **브랜드 요소 하드코딩 금지.** 색·서체는 CSS 토큰(shadcn 변수 체계), 상호·사업자 표기·정책 문구는 site_setting — 재판매 리스킨 전제.
- **전 스토어프론트 화면에 공통 푸터 필수** (루트 레이아웃 부착). 체크아웃만 축약형 허용, 관리자는 셸 하단 미니 표기.
- **접근성 KWCAG AA.** 색만으로 상태 전달 금지(품절 = 색 + 텍스트), 포커스 링 상시 가시, 터치 타겟 44px 이상, 이미지 alt 필수, 키보드만으로 구매 완주 가능, prefers-reduced-motion 존중.
- **PG 시크릿·API 키는 서버 env 원칙.** 클라이언트 번들·site_setting 평문 저장 금지.

---

## [RULE-12] DB 스키마 개선 제안 의무 — ★높은 우선순위★

**개발 단계(운영 데이터가 쌓이기 전)가 스키마를 고칠 수 있는 마지막 기회다. 어색하거나 문제 있는 스키마를 발견하면 우회 구현하지 말고 즉시 수정을 권유한다.**

- 기능 구현 중 schema.ts에서 어색한 구조·누락 컬럼·잘못된 타입·제약 부재·이상한 관계를 발견하면, **애플리케이션 코드로 우회하지 말고 멈춰서 스키마 수정을 먼저 제안한다.**
- 제안 형식: ① 발견한 문제 ② 그대로 둘 때의 비용(운영 후 마이그레이션·데이터 정합성 리스크) ③ 수정안(스키마 diff + 마이그레이션 SQL) ④ 영향 범위(코드·기존 데이터).
- 개발 단계에서는 "나중에 고치자"를 금지한다 — **지금 고치는 것이 기본값.** 운영 전환 후에는 변경 비용이 급증한다.
- 사소해 보여도 보고한다. 채택 여부는 사용자가 결정한다.
- 수정 실행 절차는 기존 규칙을 따른다: 계획·승인(RULE-4), SQL은 사용자가 직접 실행(RULE-2).

---

## [RULE-13] 임시 산출물 즉시 정리 — 깔끔한 코드베이스

**판매용 솔루션 코어다. 저장소에는 프로덕션 코드·정식 테스트·문서만 남긴다.**

- 검증·디버깅용 임시 스크립트, 일회성 테스트 파일, 스캐폴드 잔여물, 미사용 보일러플레이트는 **목적을 다하면 그때그때 정리한다.** 직접 삭제할 수 없는 것(RULE-5 승인 필요 등)은 사용자에게 정리를 권유한다.
- 임시 파일이 필요하면 프로젝트 밖(스크래치 디렉토리)에 만든다 — 프로젝트 안에 만들지 않는다.
- 작업 완료 리뷰(RULE-4 4단계) 때 "이번에 생긴 임시 산출물이 있는가"를 항상 점검한다.

---

## [RULE-14] 모듈화·캡슐화·유지보수성 — ★최고 우선순위★

**재판매 코어 제품이다. 테마 교체·기능 추가·업체별 배포가 전제이므로, 구조 품질을 기능 속도보다 우선한다.**

- **레이어 경계 준수**: UI 컴포넌트 ↔ 서버 진입점(tRPC 라우터) ↔ 도메인 서비스 ↔ DB(Drizzle) — 각 레이어의 책임을 명확히 나누고, 레이어를 건너뛰는 임포트를 금지한다 (예: 컴포넌트에서 db 직접 임포트 금지).
- **도메인 로직은 화면에서 분리**: 금액 계산·재고 차감·상태 전이·배송비 계산 같은 규칙은 화면 코드가 아니라 도메인 모듈에 캡슐화한다 — 같은 규칙이 스토어프론트/관리자/배치에서 재사용된다.
- **공개 표면 최소화**: 모듈은 필요한 것만 export하고 내부 구현을 숨긴다.
- 단, **과잉 추상화와 구분한다**: 모듈화 = 책임·경계의 분리이지 미래를 위한 추측성 유연화가 아니다. 사용처 1곳뿐인 코드의 추상화 금지(글로벌 지침 "단순함 우선")는 계속 유효하다.

---

## 요약 체크리스트

작업 전 아래를 확인한다:

- [ ] `.env` 계열 파일을 열려고 하는가? → **중단**
- [ ] DB에 직접 접근하거나 SQL을 실행하려고 하는가? (drizzle-kit push/migrate 포함, 서브에이전트 포함) → **스크립트 작성 후 전달**
- [ ] TypeScript/JavaScript/SQL 이외의 언어로 코드를 작성하려고 하는가? → **사용자에게 먼저 질문**
- [ ] 계획 없이 바로 구현하려고 하는가? → **계획 먼저 제시**
- [ ] 파일을 삭제하거나 수정하려고 하는가? → **승인 먼저 요청**
- [ ] 질문을 영어로 하려고 하는가? → **한글로 변환**
- [ ] Git 관련 행동을 하려고 하는가? (서브에이전트 포함) → **히스토리 조회 외 모두 중단**
- [ ] "작업내용 작성" 요청을 받았는가? → `C:\_Dev\_md_doc\conlab\projects\PaRaSOL\worklog\작업로그_parasolv1_YYYYMMDD.md` 생성·추가 (Markdown)
- [ ] 작업/분석 문서를 새로 생성했는가? → MEMORY.md 문서 인덱스에 키워드 항목 추가
- [ ] 대분류격(범용) 단독 변수·필드명을 쓰려 하는가? → **구체적 이름으로(꼭 필요할 때만 예외)**
- [ ] 금액·재고·주문 로직을 만지는가? → **RULE-11 도메인 규약 준수 확인**
- [ ] 화면을 구현하는가? → **해당 `.dc.html` 목업 + 핸드오프 README 규칙 먼저 확인**
- [ ] 스키마가 어색하거나 문제가 보이는가? → **우회 구현 금지, 즉시 수정 제안 (RULE-12)**
- [ ] 임시 스크립트·테스트 파일이 남아 있는가? → **그때그때 정리 또는 정리 권유 (RULE-13)**
- [ ] 도메인 로직을 화면 코드에 넣으려 하는가? → **도메인 모듈로 캡슐화 (RULE-14)**
